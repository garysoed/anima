# Typography

Core data models, canonical constant lists, and utility formatters for typography design tokens.

## Files

- [to-css-font.ts](to-css-font.ts): Utility formatter that converts a `TypographyValue` into a CSS `font` shorthand string.
- [types.ts](types.ts): Typography types, values, canonical types/sizes lists, and `TypographySet` interface.
